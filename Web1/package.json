{
    "name": "boolean-logic-solver",
    "version": "1.1.0",
    "private": true,
    "description": "Boolean Logic Solver - Truth Tables, Minterms, Maxterms and Logic Circuits",
    "scripts": {
        "dev": "vite",
        "build": "tsc && vite build",
        "preview": "vite preview",
        "typecheck": "tsc --noEmit"
    },
    "devDependencies": {
        "typescript": "^5.5.0",
        "vite": "^6.0.0"
    }
}
